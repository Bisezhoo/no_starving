# 智能食谱助手需求分析

| 项目 | 内容 |
|------|------|
| 文档版本 | v0.14 |
| 最近更新时间 | 2026-06-14 13:40:11 CST |
| 文档状态 | 需求分析已收敛，技术设计待确认 |
| 需求来源 | `任务.md`、`data_interface_testing/meal/mealdb-analysis.md`、`data_interface_testing/cocktail/cocktail-api-analysis.md`、`data_interface_testing/full_api/api-full-test-report.md`、`智能食谱助手技术设计.md` |

## 1. 摘要

本次任务是基于免费公开菜谱与饮品 API，构建一个智能食谱助手。用户通过 Chat 对话描述想吃什么，后端 Agent 调用工具搜索菜谱、获取详情、推荐饮品，并把结果以结构化卡片返回给前端展示。

接口测试数据表明，TheMealDB 与 TheCocktailDB 的详情字段存在大量 `null`、空字符串、空格和重复随机结果。因此，本需求的核心不是简单转发外部 JSON，而是由后端完成意图识别、工具调用、数据清洗、去重、错误降级和结构化输出。

## 2. 关键假设

1. 当前项目技术栈已确认为后端 Python FastAPI、前端 Vite + Vue 3 + TypeScript；本需求分析只定义业务边界、接口契约、数据处理规则和测试目标，不进入代码实现。
2. OpenRouter API Key 通过后端环境变量配置，前端不可接触密钥。
3. 全接口测试已覆盖 TheMealDB / TheCocktailDB 的 `search`、`filter`、`lookup`、`random` 和 TheMealDB `categories`；需求以 `data_interface_testing/full_api/api-full-test-report.md` 的跨接口结论为准。
4. 首版优先完成必做项：3 个 Agent Tool、多轮对话、结构化展示。加分项按阶段进入。
5. 首版目标用户可以使用中文或英文提问；后端必须识别提问语言，把查询意图转换为外部 API 更容易识别的英文参数，并按提问语言解释或翻译输出内容。
6. 首版为单用户、单 Agent、单活跃请求，不需要支持多用户、多 `session`、账号体系、并发会话合并、跨设备同步或会话隔离。
7. 菜品推荐基于当前唯一用户的轻量口味画像，不做复杂推荐系统、账号级长期画像或跨用户学习；首版使用后端本地 JSON 文件持久化画像、推荐历史和 Agent Memory，不引入数据库。

## 3. 联系人与角色

| 角色 | 职责 | 状态 |
|------|------|------|
| 产品/验收方 | 确认需求范围、加分项优先级、验收标准 | 待确认 |
| 后端开发 | 实现 Agent、Tool、外部 API 适配、校验、日志、测试 | 待确认 |
| 前端开发 | 实现 Chat 页面、结构化卡片、状态展示、前端体验校验 | 待确认 |
| 代码审查 | 由 Claude 在编码完成后检查代码 | 已由项目规则指定 |

## 4. 背景

任务要求使用 TheMealDB 和 TheCocktailDB 两个公开免费 API，结合 OpenRouter 大模型能力，构建一个能理解自然语言的食谱推荐助手。用户不需要知道具体 API 参数，只要表达“想吃鸡肉”“推荐一顿晚餐”“想要素食”之类的需求，Agent 应能选择合适工具并返回可读、可点击、可展示的结果。

为什么需要数据分析先行：接口测试材料显示外部 API 字段并不稳定。食材字段是 `strIngredient1~20` 或 `strIngredient1~15` 的编号字段，后段大量为空；用量字段常有前后空格；标签、视频、来源、多语言说明大量缺失；随机结果会重复。因此系统必须先建立统一的数据模型，前端只消费清洗后的结构。

## 5. 目标与成功标准

### 5.1 业务目标

1. 用户能通过自然语言完成菜谱搜索、菜谱详情查看、饮品搜索和餐食搭配推荐。
2. Agent 能自主决定是否调用 `search_meals`、`get_meal_detail`、`search_cocktails`。
3. 前端能展示结构化结果，包括图片、名称、分类、国家/地区、食材/配料、做法摘要和来源信息。
4. 系统面对外部 API 空值、重复、超时、无结果时能优雅降级。
5. 系统能识别用户本轮提问语言，并用相同语言生成主要回复、结果解释和必要翻译；外部 API 原始英文名称保留为辅助信息。
6. 系统能从对话和显式配置中维护轻量口味画像，并基于画像解释推荐原因。

### 5.2 成功标准

| 编号 | 成功标准 | 验证方式 |
|------|----------|----------|
| S1 | 用户发送菜谱搜索意图后，Agent 能调用菜谱搜索工具并返回结构化菜谱卡片 | 后端集成测试 + 前端手工验收 |
| S2 | 用户要求详情时，Agent 能根据菜谱 ID 调用详情工具，展示食材清单和步骤 | Tool 单元测试 + Chat 集成测试 |
| S3 | 用户请求配饮时，Agent 能调用饮品工具并返回饮品卡片 | Tool 单元测试 + Chat 集成测试 |
| S4 | 食材、用量、标签、步骤中的空值、空格和换行被统一清洗 | 数据归一化单元测试 |
| S5 | 外部 API 返回重复或空结果时，页面不崩溃，回复说明清楚 | Mock API 异常测试 |
| S6 | OpenRouter 或外部 API 超时时，后端返回统一错误格式 | 接口测试 |
| S7 | 中文输入能被转换为可用英文查询词，回复主体、解释和摘要为中文 | Agent 路由测试 + 集成测试 |
| S8 | 当前唯一会话内，上下文和饮食偏好能在多轮对话中延续 | 多轮对话测试 |
| S9 | 英文输入时回复主体为英文；中英混合输入时按主要提问语言输出 | Agent 语言检测测试 + 集成测试 |
| S10 | `filter` 摘要接口能按需补 `lookup` 详情，无结果格式差异能被统一处理 | Tool 集成测试 |
| S11 | Agent 能提取、更新并应用轻量口味画像，推荐结果给出画像匹配理由 | 画像单元测试 + Chat 集成测试 |
| S12 | Agent 能基于纵向推荐历史避免短期重复推荐，例如连续多次推荐同一主食材 | 推荐历史单元测试 + Chat 集成测试 |

## 6. 用户与场景

| 用户场景 | 示例输入 | 期望行为 |
|----------|----------|----------|
| 按关键词找菜 | `我想吃 chicken` | Agent 搜索菜谱，返回若干菜谱卡片 |
| 按食材找菜 | `家里有鸡蛋和番茄，能做什么` | Agent 优先按主要食材搜索，必要时追问 |
| 查看菜谱详情 | `第一个怎么做` | Agent 使用上一轮上下文中的菜谱 ID 获取详情 |
| 推荐一顿饭 | `给我推荐一顿晚餐` | Agent 推荐主菜，并搭配一个饮品 |
| 饮食偏好 | `想吃素一点的` | Agent 优先按 Vegetarian/Vegan 或相关关键词过滤 |
| 饮品推荐 | `推荐一杯清爽的鸡尾酒` | Agent 搜索饮品，展示配料、杯型和制作说明 |
| 中文关键词查询 | `我想吃鸡肉，不要太辣` | Agent 提取偏好，将核心食材转换为英文查询词，如 `chicken` |
| 会话偏好延续 | `我想吃素` 后再问 `再推荐一个` | 当前唯一会话内继续沿用素食偏好 |
| 英文提问 | `I want a light chicken dinner` | Agent 使用英文回复，并解释推荐理由 |
| 中英混合提问 | `想吃 chicken，配什么 drink` | Agent 识别主要语言为中文，用中文解释，Tool 参数保持英文 |
| 画像提取 | `我不吃香菜，喜欢清淡一点` | Agent 更新轻量口味画像，后续推荐避开香菜并偏清淡 |
| 画像解释 | `再推荐一道适合我的` | Agent 基于当前画像推荐，并说明匹配了哪些偏好 |
| 避免重复推荐 | `这周别总给我推荐牛肉` | Agent 查询推荐历史，降低近期牛肉菜的推荐优先级 |

## 7. 价值主张

1. 用户不用学习 API 查询方式，用自然语言就能得到食谱建议。
2. 推荐结果不是原始 JSON，而是清洗后的结构化内容，可直接阅读和操作。
3. 系统能把菜谱和饮品组合起来，降低用户“吃什么、配什么”的选择成本。
4. 后端统一处理 API 不稳定字段，前端展示更稳定，后续扩展也更简单。

## 8. 功能需求

### 8.1 后端 Agent

| 编号 | 需求 | 优先级 |
|------|------|--------|
| B1 | 使用 OpenRouter API 调用大语言模型 | P0 |
| B2 | 支持多轮对话，上下文至少能识别“第一个”“刚才那个”等引用 | P0 |
| B3 | Agent 根据用户输入自主选择 Tool | P0 |
| B4 | 返回自然语言回复、结构化卡片数据、可选 Tool 调用轨迹 | P0 |
| B5 | 对外部 API 做超时、失败、空结果处理 | P0 |
| B6 | 对同一轮结果按 ID 去重 | P1 |
| B7 | 支持餐食搭配推荐：主菜 + 配饮 | P1 |
| B8 | 支持饮食偏好过滤：素食、纯素、地区菜系 | P1 |
| B9 | 支持中文输入理解，将搜索关键词、食材、地区、分类转换为英文 Tool 参数 | P0 |
| B10 | 维护当前唯一会话的多轮上下文、候选卡片和轻量口味画像 | P0 |
| B11 | 明确非目标：首版不支持多用户、多 `session`、账号级长期画像 | P0 |
| B12 | 检测本轮提问语言，并按提问语言生成回复、结果解释、卡片摘要和必要翻译 | P0 |
| B13 | 识别 `search`/`lookup`/`random` 详情结果与 `filter` 摘要结果的字段差异，必要时自动补详情 | P0 |
| B14 | 维护轻量口味画像，并在菜品推荐、筛选和推荐理由中使用画像信号 | P0 |
| B15 | 维护纵向推荐历史，用于短期去重和主食材/分类/菜系轮换 | P1 |

### 8.2 必需 Tool

| Tool | 输入 | 行为 | 输出 |
|------|------|------|------|
| `search_meals` | `query`、`ingredient`、`category`、`area`、`limit` | 调用 TheMealDB 搜索或筛选接口；`filter` 返回 5 字段摘要，需按 Top N 调用 `lookup` 补详情 | 标准化菜谱摘要或详情列表 |
| `get_meal_detail` | `idMeal` | 调用 TheMealDB `lookup.php?i={id}` | 标准化菜谱详情 |
| `search_cocktails` | `query`、`ingredient`、`limit` | 调用 TheCocktailDB 搜索或筛选接口；`filter` 返回 3 字段摘要，需按 Top N 调用 `lookup` 补详情 | 标准化饮品摘要或详情列表 |

说明：TheMealDB 的 `filter` 接口返回 `idMeal`、`strMeal`、`strMealThumb`、`strArea`、`strCountry` 5 个字段；TheCocktailDB 的 `filter` 接口只返回 `idDrink`、`strDrink`、`strDrinkThumb` 3 个字段。为满足结构化展示，后端应对 Top N 结果调用 `lookup` 补齐详情，但需要限制数量，避免响应过慢。

无结果处理必须兼容三种格式：TheMealDB 所有接口返回 `{"meals": null}`，TheCocktailDB `search`/`lookup` 返回 `{"drinks": null}`，TheCocktailDB `filter` 返回 `{"drinks": "no data found"}`。后端统一转为标准空数组和清晰回复文案。

Tool 调用前必须做语言处理：Agent 先检测提问语言，再提取意图、食材、菜系、饮品偏好，并转换为英文 API 参数。转换结果和检测到的输出语言应进入 Tool 调用轨迹，便于调试。例如用户输入“鸡肉晚餐”时，`search_meals` 的 `ingredient` 可使用 `chicken`，回复和卡片摘要使用中文。

### 8.3 前端 Web 界面

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F1 | Chat 对话界面，包含消息列表、输入框、发送按钮、加载状态 | P0 |
| F2 | 菜谱卡片展示图片、名称、分类、国家/地区、食材摘要 | P0 |
| F3 | 详情展示完整食材/用量列表和烹饪步骤 | P0 |
| F4 | 饮品卡片展示图片、名称、分类、酒精类型、杯型、配料 | P0 |
| F5 | 错误、无结果、加载中状态可见且文案清楚 | P0 |
| F6 | Tool 调用链路可视化 | P1 |
| F7 | 流式输出 | P2 |
| F8 | 卡片支持展示英文原名和按提问语言生成的摘要，避免把机器翻译误当作原始数据 | P0 |

前端只负责交互展示、即时体验校验和 API 调用。意图识别、业务校验、字段清洗、状态流转和权限/密钥处理全部在后端完成。

### 8.4 轻量口味画像

首版使用当前唯一会话内的轻量口味画像，服务于菜品推荐和多轮偏好延续。画像由后端维护，来源包括用户自然语言表达、可选表单输入和 Agent 对上一轮偏好的结构化提取。

| 字段 | 类型 | 来源 | 用途 |
|------|------|------|------|
| `dietaryRestrictions` | string[] | 用户表达或表单 | 过滤素食、纯素等饮食限制 |
| `likedIngredients` | string[] | 用户表达 | 推荐时优先匹配喜欢的食材 |
| `dislikedIngredients` | string[] | 用户表达 | 推荐时避开不喜欢或不吃的食材 |
| `preferredCuisines` | string[] | 用户表达或地区偏好 | 映射 TheMealDB `area` / `country` 查询 |
| `flavorPreferences` | string[] | 用户表达 | 记录清淡、 spicy、甜、酸等口味倾向 |
| `allowAlcohol` | boolean | 用户表达或默认策略 | 决定是否推荐含酒精饮品 |
| `lastIntent` | string | Agent 提取 | 支持“再推荐一个”“换个清淡的”等追问 |
| `recommendationHistory` | RecommendationRecord[] | 每次推荐后写入 | 避免短期重复推荐，支持主食材/分类/菜系轮换 |

画像更新规则：

1. 显式表达优先于推断，例如“我不吃牛肉”立即写入 `dislikedIngredients`。
2. 新消息可覆盖旧偏好，例如“今天可以喝酒”覆盖本轮饮品推荐的 `allowAlcohol`。
3. 画像只在当前唯一会话内有效，不做账号级长期保存。
4. 推荐结果必须能解释至少一个匹配信号，例如“避开了香菜”“优先选择了素食分类”。
5. 画像只做偏好层面的辅助推荐，不做过敏、医疗或营养精确判断。
6. 推荐历史采用纵向数组对象，不使用并列数组，避免菜品、食材、分类和时间错位。
7. 推荐历史默认只做短期去重；如果要解决“连续 4 天牛肉”这类跨天问题，需要在设计阶段确认是否保留到本地存储或后端存储。

## 9. 数据接口分析结论

### 9.0 全接口测试总览

`data_interface_testing/full_api/api-full-test-report.md` 对 11 个接口做了 30 组参数采样，结论如下：

| 接口类型 | TheMealDB | TheCocktailDB | 需求影响 |
|----------|-----------|---------------|----------|
| `search` | 返回 54 字段详情，30 词搜索得到 332 条去重结果 | 返回 51 字段详情，30 词搜索得到 162 条去重结果 | 可直接标准化为详情卡片 |
| `filter` | 返回 5 字段摘要，包含 `idMeal`、名称、图片、`strArea`、`strCountry` | 返回 3 字段摘要，只包含 `idDrink`、名称、图片 | 结构化展示前需要按 Top N 补 `lookup` |
| `lookup` | 返回 54 字段详情 | 返回 51 字段详情 | 详情页和 `filter` 补全的权威来源 |
| `random` | 返回 54 字段详情，可能重复 | 返回 51 字段详情，可能重复 | 推荐时需按 ID 去重 |
| `categories` | 返回 14 个分类，4 字段 | 不适用 | 可用于分类过滤候选项 |

全接口测试还确认：两个 API 的 `filter` 参数不区分大小写，但后端仍应统一转换、URL 编码并记录实际调用参数。

### 9.1 TheMealDB

| 数据点 | 需求影响 |
|--------|----------|
| 详情共 54 个字段，12 个从未缺失 | 只把核心字段作为稳定展示字段 |
| `search`、`lookup`、`random` 返回 54 字段详情 | 可直接清洗为详情卡片 |
| `filter.php?i/c/a` 返回 5 字段摘要 | 列表可先展示基础信息；需要食材/步骤时必须补 `lookup` |
| `categories.php` 返回 14 个分类，字段完整 | 可作为分类筛选的候选来源 |
| `strMealAlternate`、`strImageSource`、`strCreativeCommonsConfirmed` 永远缺失 | 不进入首版 UI |
| `strIngredient1~3` 从未缺失，后续食材缺失率逐步升高 | 遍历 `1~20`，跳过空值，不固定展示数量 |
| `strMeasure` 常带前后空格 | 后端统一 `trim()` |
| `strInstructions` 99/100 含换行，格式不统一 | 后端保留原文并拆成可渲染段落，前端不要自行解析步骤编号 |
| `strTags` 65% 缺失 | 标签仅作为可选展示 |
| 全接口测试中 `strCountry` 零缺失，`strArea` 在 `search/filter/random` 中可能缺失 | 首选 `strCountry`，缺失时回退 `strArea` |
| 无结果返回 `{"meals": null}` | 后端统一转为空数组 |
| `search.php?s=` 空参数会返回 25 条默认数据 | 只有明确做“默认推荐”时才可使用，普通搜索不得误传空字符串 |
| 随机接口 100 次出现 5 个重复 ID | 结果列表按 `idMeal` 去重 |

### 9.2 TheCocktailDB

| 数据点 | 需求影响 |
|--------|----------|
| 详情共 51 个字段，11 个从未缺失 | 只依赖 `idDrink`、`strDrink`、`strDrinkThumb`、`strCategory`、`strAlcoholic`、`strGlass`、`strInstructions` 等核心字段 |
| `search`、`lookup`、`random` 返回 51 字段详情 | 可直接清洗为详情卡片 |
| `filter.php?i` 只返回 3 字段摘要 | 搜索饮品配料时必须按 Top N 调 `lookup` 才能展示分类、酒精类型、杯型、配料和步骤 |
| `strIngredient10~15`、`strMeasure10~15` 在采样中永远缺失 | 仍遍历 `1~15`，但 UI 不预留固定 15 行 |
| `strInstructionsZH-HANS`、`strInstructionsZH-HANT` 在全接口测试中 99%~100% 缺失 | 中文输出不得依赖 API 中文字段，必须由 Agent 基于英文说明解释或翻译 |
| `strIBA` 75%~86% 缺失，`strTags` 50%~75% 缺失 | IBA 与标签仅作为可选展示 |
| `strAlcoholic`、`strGlass`、`strCreativeCommonsConfirmed` 在详情接口中零缺失 | 饮品卡片可稳定展示酒精类型和杯型；CC 字段可作为可选来源信息 |
| `strMeasure` 有大量前后空格 | 后端统一 `trim()` |
| `strGlass` 存在大小写不一致 | 展示原值，筛选或聚合时再做归一化 |
| `search/lookup` 无结果返回 `{"drinks": null}`，`filter` 无结果返回 `{"drinks": "no data found"}` | 后端统一判空，不能只判断数组或 null |
| `search.php?s=` 空参数返回 null | 饮品默认推荐不得使用空搜索，应使用 `random.php` 或明确关键词 |
| 随机接口 100 次出现 5 个重复 ID | 结果列表按 `idDrink` 去重 |

### 9.3 统一标准化模型

后端应把外部 API 统一转为内部展示模型，避免前端处理编号字段。

```ts
type IngredientItem = {
  name: string;
  measure?: string;
};

type TasteProfile = {
  dietaryRestrictions: string[];
  likedIngredients: string[];
  dislikedIngredients: string[];
  preferredCuisines: string[];
  flavorPreferences: string[];
  allowAlcohol?: boolean;
  lastIntent?: string;
  recommendationHistory: RecommendationRecord[];
};

type RecommendationRecord = {
  itemType: "meal" | "cocktail";
  itemId: string;
  title: string;
  recommendedAt: string;
  mainIngredients: string[];
  category?: string;
  cuisine?: string;
  matchReasons: string[];
};

type MealCard = {
  type: "meal";
  id: string;
  detailLevel: "summary" | "detail";
  title: string;
  localizedTitle?: string;
  localizedLanguage?: string;
  imageUrl: string;
  category?: string;
  country?: string;
  tags: string[];
  ingredients: IngredientItem[];
  instructions?: string[];
  localizedSummary?: string;
  localizedInstructions?: string[];
  matchReasons?: string[];
  sourceUrl?: string;
  youtubeUrl?: string;
};

type CocktailCard = {
  type: "cocktail";
  id: string;
  detailLevel: "summary" | "detail";
  title: string;
  localizedTitle?: string;
  localizedLanguage?: string;
  imageUrl: string;
  category?: string;
  alcoholic?: string;
  glass?: string;
  tags: string[];
  ingredients: IngredientItem[];
  instructions?: string[];
  localizedSummary?: string;
  localizedInstructions?: string[];
  matchReasons?: string[];
};
```

## 10. API 契约建议

### 10.1 Chat 接口

`POST /api/chat/stream`

首版唯一对外 Chat 接口为流式接口，响应类型为 `text/event-stream`。暂不实现 `POST /api/chat/message` 非流式接口；如果后续部署环境证明 SSE 被网关或代理缓冲，再评估新增非流式接口。

请求体：

| 字段 | 类型 | 必填 | 校验规则 |
|------|------|------|----------|
| `message` | string | 是 | `trim` 后长度 1~1000 |

画像和 Agent Memory 不由前端随请求传入。后端从内存状态或本地 JSON 文件读取 `TasteProfile`、推荐历史和 Agent Memory，并在用户通过自然语言表达偏好或产生新候选结果后更新本地 JSON。

前置校验失败或当前已有请求处理中时，不建立 SSE 流，直接返回统一 HTTP JSON 错误：空消息、超长消息返回 `400`；`conversation_lock` 已被占用时返回 `409` 和“当前已有请求处理中，请稍后再试”。SSE `error` 事件只用于流已经建立后的运行期异常或部分能力失败。

流式响应事件至少包含 `meta`、`delta`、`tool_call`、`tool_result`、`card`、`profile_update`、`done`、`error`。`done` 事件携带最终完整快照，包括 `detectedLocale`、`reply`、`tasteProfile`、`profileUpdates`、`cards`、`toolCalls` 和 `suggestions`。

SSE 请求建立失败时，前端展示连接失败；SSE 中途断开且未收到 `done` 时，前端保留已收到内容并提示“生成中断，请重试”。首版不自动改走非流式接口，避免重复 Tool 调用、重复画像更新和幂等复杂度。

### 10.2 后端校验规则

| 对象 | 校验规则 |
|------|----------|
| 用户消息 | 非空，最大 1000 字符，去除首尾空格 |
| Tool `limit` | 整数，范围 1~10，默认 5 |
| 菜谱 ID / 饮品 ID | 非空数字字符串，最大 20 位 |
| 搜索关键词 | 非空时最大 100 字符，调用外部 API 前 URL 编码；普通搜索不得把空字符串透传给外部 API |
| 后端本地画像 JSON | 数组长度、单项长度、布尔类型和推荐历史条目全部由后端校验；去重、去空格、忽略空字符串；文件不存在时使用空画像 |
| 外部 API 响应 | 兼容数组、`null`、`"no data found"`；统一标准化为空数组或卡片列表 |

### 10.3 语言策略

| 项目 | 首版策略 |
|------|----------|
| 用户输入 | 支持中文、英文和中英混合；后端检测本轮主要提问语言 |
| 外部 API 查询 | 优先使用英文关键词、英文食材、英文分类和地区参数 |
| 输出语言 | 不接收 `languagePreference` 或 `locale` 入参；按本轮主要提问语言输出 |
| Agent 回复 | 使用输出语言解释推荐理由、筛选依据、注意事项和下一步建议 |
| 卡片标题 | 保留外部 API 英文原名，可附加按输出语言生成的名称或摘要 |
| 做法说明 | 保留原始英文步骤；按输出语言提供解释、摘要或步骤翻译。首版可做实用翻译，不承诺烹饪术语级人工校对 |
| 中英混合 | 以主要提问语言为输出语言；若难以判断，优先沿用当前唯一会话最近一次语言 |
| 风险提示 | 机器翻译或摘要可能不等同原始菜谱，应避免用于过敏、医疗、营养精确判断 |

### 10.4 单用户、单会话与画像作用域

| 项目 | 首版策略 |
|------|----------|
| 用户身份 | 单用户匿名使用，不做登录注册 |
| 会话数量 | 单 Agent 会话，不支持多 `session` 并行 |
| 会话标识 | 不需要对外暴露 `sessionId` |
| 并发边界 | 单活跃请求；如果两个页面同时发送消息，第二个请求应返回“当前已有请求处理中，请稍后再试” |
| 多轮上下文 | 当前唯一会话内保存最近消息、候选卡片、轻量口味画像和 Agent Memory |
| 画像作用域 | 当前会话级轻量口味画像，使用 `TasteProfile` 字段 |
| 画像与 Memory 生命周期 | 技术设计默认由后端本地 JSON 文件保存单用户画像、推荐历史和 Agent Memory；后端每轮读取、校验、合并并写回 |
| 非目标 | 首版不做多用户隔离、账号级长期画像、跨设备同步、家庭成员多画像 |

## 11. 非功能需求

| 类别 | 要求 |
|------|------|
| 性能 | Chat 首包在合理时间返回；补详情时限制 Top N，避免一次查询放大过多外部请求 |
| 可靠性 | 外部 API 设置超时；失败时返回可理解文案；可对详情结果按 ID 做短期缓存 |
| 安全 | OpenRouter Key 仅在后端环境变量；日志不记录完整密钥和敏感 Header |
| 幂等 | 查询类请求不产生业务状态；当前会话上下文更新只影响唯一 Agent 会话 |
| 并发控制 | 首版只允许单活跃请求；`conversation_lock` 必须在正常完成、异常、超时和客户端断开时释放，避免死锁 |
| 可观测性 | 核心链路记录请求 ID、Tool 名称、外部 API、耗时、结果数量、异常类型 |
| 可维护性 | 所有外部 API 字段清洗集中在后端适配层，前端不读取 `strIngredient1` 这类原始字段 |
| 隐私 | 日志不记录完整对话；不记录 OpenRouter Key、外部 API Header 或完整用户输入 |
| 外部请求放大控制 | `filter` 补 `lookup` 详情时只处理 Top N，默认不超过 `limit`，并记录补详情数量和失败数量 |

## 12. 测试设计

### 12.1 单元测试

| 模块 | 用例 |
|------|------|
| Meal 标准化 | 处理 `null`、`""`、`" "`、`"null"`；食材和用量配对；`strCountry` 优先 |
| Cocktail 标准化 | 遍历 `1~15`；忽略缺失配料；保留 `strAlcoholic`、`strGlass` |
| Tool 参数校验 | 空关键词、超长关键词、非法 ID、`limit > 10` 均被拒绝或修正 |
| Agent 路由 | 菜谱搜索、详情追问、饮品搜索、晚餐搭配分别选择正确 Tool |
| 语言转换 | 中文食材、菜系、饮品偏好能转换为合理英文 Tool 参数 |
| 输出语言 | 中文、英文、中英混合输入能得到对应语言的解释、摘要和必要翻译 |
| 会话画像 | 当前唯一会话内延续饮食偏好和候选卡片引用；输出语言始终按本轮提问语言检测 |
| 画像提取 | “不吃香菜”“喜欢清淡”“想吃日式”能更新对应 `TasteProfile` 字段 |
| 画像覆盖 | “今天可以喝酒”“这次不要鸡肉”能覆盖或追加本轮画像信号 |
| 推荐理由 | 推荐卡片能生成与画像相关的 `matchReasons` |
| 推荐历史 | 推荐后追加 `RecommendationRecord`，超过上限时丢弃最旧记录 |
| 防重复策略 | 相同 `itemId` 硬去重；近期高频主食材/分类/菜系软降权 |
| 空结果归一化 | `meals: null`、`drinks: null`、`drinks: "no data found"` 都转为空数组 |
| 摘要/详情层级 | `filter` 返回卡片标记为 `summary`，补 `lookup` 后标记为 `detail` |

### 12.2 集成测试

| 场景 | 验证点 |
|------|--------|
| TheMealDB 搜索成功 | 返回标准化 MealCard，不暴露原始编号字段 |
| TheMealDB 详情成功 | 返回完整食材列表和步骤段落 |
| TheCocktailDB 搜索成功 | 返回标准化 CocktailCard |
| TheMealDB filter 补详情 | `filter.php?i/c/a` 返回摘要后，仅对 Top N 调用 `lookup` 并返回详情卡片 |
| TheCocktailDB filter 补详情 | `filter.php?i` 返回 3 字段摘要后，仅对 Top N 调用 `lookup` 并返回详情卡片 |
| 外部 API 空结果 | `meals: null`、`drinks: null`、`drinks: "no data found"` 都返回 `code=200`、`cards=[]`，回复说明无结果 |
| 外部 API 超时 | 返回统一错误结构，前端展示降级文案 |
| 随机/列表重复 ID | 去重后返回 |
| 空搜索保护 | TheMealDB 空搜索只在默认推荐场景使用；TheCocktailDB 不使用空搜索做推荐 |
| 中文输入搜索 | `我想吃鸡肉` 能触发英文参数查询并返回中文回复 |
| 英文输入搜索 | `I want chicken` 能返回英文回复和英文解释 |
| 中英混合输入 | `想吃 chicken` 能识别主要语言并按该语言输出 |
| 单会话多轮 | 先设置素食偏好，再追问“再推荐一个”，能沿用当前会话画像 |
| 画像驱动推荐 | 先设置“不吃香菜、偏清淡”，再请求推荐，结果和推荐理由体现该画像 |
| 短期防重复 | 最近推荐多次牛肉后，再请求泛化推荐，应降低牛肉菜优先级并说明换口味 |

### 12.3 前端测试

| 场景 | 验证点 |
|------|--------|
| 输入框 | 空消息不可提交，超长消息有提示 |
| 加载态 | 请求中按钮不可重复提交或有明确状态 |
| 卡片展示 | 图片、标题、标签、食材列表不因缺失字段崩溃 |
| 错误态 | API 失败时展示后端返回文案 |
| 多轮上下文 | 点击或引用“第一个”后能请求详情 |

## 13. 非目标

1. 首版不做用户登录、收藏、购物清单、营养计算和支付。
2. 首版不承诺外部 API 存在完整本地化数据。解释、摘要和翻译由 Agent 基于原始内容生成。
3. 首版不做复杂饮食禁忌医学建议，只做用户偏好层面的过滤和提醒。
4. 首版不要求所有加分项同时完成。流式输出和 Tool 链路可视化可后置。
5. 首版不做账号体系、多用户、多 `session`、用户级长期偏好、跨设备同步和多成员画像。
6. 首版不承诺所有菜名、步骤、配料都有人工级专业翻译。

## 14. 发布分期

| 阶段 | 范围 | 验收重点 |
|------|------|----------|
| Phase 1 | Chat、3 个 Tool、多轮上下文、标准化卡片、错误降级 | 必做项全部通过 |
| Phase 2 | 晚餐搭配、素食/地区偏好、结果去重优化、日志完善 | 加分项中高价值功能 |
| Phase 3 | Tool 调用链路可视化、流式输出、UI 打磨、缓存策略 | 体验增强 |

## 15. 开放问题

1. OpenRouter 使用的具体模型、预算、上下文长度和响应速度目标是否已确定。
2. 是否允许默认推荐含酒精饮品，还是需要默认优先无酒精。
3. 结果本地化的展示粒度需要在实现计划中定稿：默认展示摘要翻译，还是默认展开完整步骤翻译。
4. 是否需要在前端展示或手动编辑轻量口味画像。
5. 前端是否需要移动端优先适配。
6. 免费 API 的缓存 TTL、失败重试次数、超时时间和降级文案需要在实现计划中定稿。
7. 外部图片和来源链接是否需要在卡片中展示版权/来源提示。
8. “饮食偏好”是否只包含素食/纯素/地区/酒精，还是要覆盖过敏原、宗教饮食或低糖低脂等复杂规则。
9. TheMealDB `categories.php` 是否需要在首版前端展示为分类快捷筛选。

## 16. 本次需求分析执行计划

| 序号 | 任务 | 状态 | 备注 |
|------|------|------|------|
| 1 | 阅读 `任务.md`，提炼必做项与加分项 | ✅ 已完成 | 已识别后端 Agent、3 个 Tool、Web Chat 和结构化展示要求 |
| 2 | 阅读 TheMealDB 接口测试分析 | ✅ 已完成 | 已提炼字段缺失、空值、去重和展示约束 |
| 3 | 阅读 TheCocktailDB 接口测试分析 | ✅ 已完成 | 已提炼配料字段、说明字段、酒精类型和缺失字段约束 |
| 4 | 形成需求分析文档 | ✅ 已完成 | 初版文档 v0.1 |
| 5 | 等待需求确认并进入设计文档阶段 | ✅ 已完成 | 已进入 FastAPI + Vite 技术设计 |
| 6 | 补充语言转换边界 | ✅ 已完成 | v0.2 已明确中文输入、英文查询、中文回复策略 |
| 7 | 补充多用户与偏好作用域边界 | ✅ 已完成 | v0.2 曾定义匿名会话级偏好，已被 v0.4 单用户单会话边界覆盖 |
| 8 | 补充按提问语言解释/翻译输出 | ✅ 已完成 | v0.3 已明确语言检测、输出语言选择、摘要与步骤翻译策略 |
| 9 | 收敛为单用户单会话边界 | ✅ 已完成 | v0.4 已移除多用户、多 session、sessionId 隔离要求 |
| 10 | 根据 full_api 全接口测试修正文档 | ✅ 已完成 | v0.5 已补充 filter 摘要字段、lookup 补详情、空结果格式和空搜索差异 |
| 11 | 明确菜品推荐使用轻量口味画像 | ✅ 已完成 | v0.6 已定义 TasteProfile、画像更新规则、API 契约和测试要求 |
| 12 | 从轻量口味画像移除语言偏好字段 | ✅ 已完成 | v0.7 已移除 languagePreference，语言输出改为完全按本轮提问语言检测 |
| 13 | 推荐历史改为纵向数组对象 | ✅ 已完成 | v0.8 已新增 RecommendationRecord[]，用于短期去重和多样性轮换 |
| 14 | 确认技术栈为 Python FastAPI + Vite | ✅ 已完成 | v0.9 已同步技术栈边界 |
| 15 | 移除首版 Chat 请求中的显式语言入参 | ✅ 已完成 | v0.9 已明确不接收 `locale`，按本轮 `message` 检测输出语言 |
| 16 | 生成技术设计文档 | ✅ 已完成 | 已生成 `智能食谱助手技术设计.md`，待确认后进入实现计划 |
| 17 | 收敛首版 Chat 接口为流式接口 | ✅ 已完成 | v0.10 已明确首版只实现 `POST /api/chat/stream`，非流式接口后置 |
| 18 | 确认画像和推荐历史使用后端本地 JSON 持久化 | ✅ 已完成 | v0.11 已明确首版不引入数据库 |
| 19 | 确认 Agent Memory 持久化和单活跃请求锁 | ✅ 已完成 | v0.12 已明确 `agent-memory.json` 与 `conversation_lock` 死锁防护 |
| 20 | 确认前端技术栈为 Vite + Vue 3 + TypeScript | ✅ 已完成 | v0.13 已替换此前 React 口径 |
| 21 | 确认前置错误与 SSE 运行期错误边界 | ✅ 已完成 | v0.14 已明确参数错误和锁冲突走 HTTP JSON，流建立后的异常走 SSE `error` |

✅ 本次需求分析计划已完成。后续进入设计和实现前，应先更新设计文档与测试用例。

## 17. 变更记录

| 版本 | 时间 | 变更 |
|------|------|------|
| v0.1 | 2026-06-14 10:27:36 CST | 基于任务说明和接口测试数据生成初版需求分析 |
| v0.2 | 2026-06-14 10:59:49 CST | 补充中文输入/英文接口语言转换策略、多用户会话隔离、偏好作用域和遗留开放问题 |
| v0.3 | 2026-06-14 11:11:32 CST | 确认按提问者使用语言输出解释或翻译，补充语言检测、输出语言选择、本地化卡片字段和测试要求 |
| v0.4 | 2026-06-14 11:14:00 CST | 明确 Agent 不需要支持多用户、多 session，移除 sessionId 契约和会话隔离测试，改为当前唯一会话内维护上下文与轻量口味画像 |
| v0.5 | 2026-06-14 11:21:08 CST | 根据全接口测试报告修正接口边界：filter 摘要字段差异、lookup 补详情、空结果格式、空搜索行为、TheCocktailDB 中文字段不可依赖 |
| v0.6 | 2026-06-14 11:44:18 CST | 明确菜品推荐基于当前唯一会话内的轻量口味画像，新增 TasteProfile 字段、画像更新规则、推荐理由和相关测试 |
| v0.7 | 2026-06-14 11:48:07 CST | 根据确认移除 TasteProfile 中的 languagePreference，语言输出不进入画像，改为按本轮提问语言检测 |
| v0.8 | 2026-06-14 11:52:14 CST | 将推荐历史设计为纵向 RecommendationRecord[]，支持短期防重复、主食材/分类/菜系轮换，并明确跨天防重复需要持久化策略 |
| v0.9 | 2026-06-14 12:11:57 CST | 确认技术栈为 Python FastAPI + Vite，移除首版 Chat 请求中的显式 `locale` 入参，并同步单浏览器画像快照策略 |
| v0.10 | 2026-06-14 12:24:23 CST | 收敛首版 Chat 接口为 `POST /api/chat/stream`，移除首版非流式接口，SSE 中断时由前端提示重试 |
| v0.11 | 2026-06-14 13:15:06 CST | 确认画像和推荐历史使用后端本地 JSON 简单持久化，首版不引入数据库；Chat 请求体移除 `tasteProfile` 入参 |
| v0.12 | 2026-06-14 13:24:37 CST | 确认 Agent Memory 也使用后端本地 JSON 持久化，并补充单活跃请求、并发拒绝和 `conversation_lock` 死锁防护要求 |
| v0.13 | 2026-06-14 13:35:45 CST | 确认前端技术栈为 Vite + Vue 3 + TypeScript，替换此前 Vite React 口径 |
| v0.14 | 2026-06-14 13:40:11 CST | 明确 Chat 前置校验失败和单活跃请求冲突不建立 SSE 流，分别返回 HTTP `400`/`409` JSON 错误；SSE `error` 仅用于流建立后的运行期异常 |
